{
  "name": "swe-portfolio-starter",
  "version": "1.0.0",
  "description": "A portfolio starter for your flying start",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "live-server --host='localhost' --port='3000'"
  },
  "author": "multiverse",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
